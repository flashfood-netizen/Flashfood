# CLAUDE.md — Flashordo

نظام طلب ودفع وتحضير للمطاعم في الجزائر. الواجهة عربية RTL بالكامل.
اقرأ هذا الملف قبل أي تعديل. القرارات هنا نهائية ومتّفق عليها مع المالك.

---

## 0) قواعد لا تُكسر

هذه ثوابت المشروع. أي كود يخالفها خطأ حتى لو عمل.

| # | القاعدة |
|---|---|
| 1 | **المخزون لا يصبح سالباً أبداً.** |
| 2 | **لا خصم من المخزون قبل تأكيد الدفع.** المطبخ لا يرى الطلب قبله إطلاقاً. |
| 3 | **سعر البيع ≥ تكلفة المواد.** يُرفض حفظ أي منتج يُباع بخسارة. |
| 4 | **رصيد العامل لا يصبح سالباً.** الدفع لا يتجاوز المستحقّ. |
| 5 | الطلب غير المدفوع يُلغى بعد **15 دقيقة** دون أن يمسّ المخزون. |
| 6 | **الأرباح = المبيعات − تكلفة المواد − الأجور**، من الطلبات المدفوعة وحدها. |
| 7 | **المسمّيات:** تُقبل الأرقام داخل الاسم (`بيتزا 30 سم`)، ويُرفض الاسم المكوّن من أرقام فقط (`123`). |
| 8 | **الكميات والأسعار أرقام فقط.** |
| 9 | الاسم في كل الصفحات **`flashordo`** — لا `flashfood` ولا `foodordo`. |

> القواعد 1–6 **يجب أن تُفرض في قاعدة البيانات**، لا في الواجهة. الواجهة راحة للمستخدم فقط.

---

## 1) الأمان — الأهم في هذا المشروع

### المبدأ الحاكم
> كل تحقّق في الواجهة زينة. من يفتح أدوات المطوّر يستدعي Supabase مباشرة ويتخطّاه.
> **الحماية الحقيقية في RLS وقيود القاعدة ودوال السيرفر.**

### عبر الرابط
- الخطر ليس `/admin`، بل أن يفتح تاجرٌ `/restaurant/57` وهو صاحب 12.
- الحل ليس إخفاء المعرّف — بل أن يُرجع كل استعلام **صفر صفوف** بفضل RLS مهما كان المعرّف.
- `?next=` يقبل **المسارات الداخلية فقط**. لا إعادة توجيه لنطاق خارجي.
- لا توكن ولا معرّف حسّاس في الرابط (يُسجَّل في تاريخ التصفح وسجلّات السيرفر).

### عبر المدخلات
- عميل Supabase يرسل استعلامات مُعامَلة، وReact يهرّب النصّ عند العرض.
- **`dangerouslySetInnerHTML` ممنوع تماماً في هذا المشروع.**
- كل قاعدة تحقّق في الواجهة تُكرَّر كـ `CHECK constraint` في القاعدة.
- التحقّق بقائمة محارف مسموحة، **لا** بمحاولة "كشف الكود".

### عبر الصور (وصل الدفع وصور المنتجات)
أخطر مدخل في النظام لأنه ملف يرفعه غريب:

1. **امنع SVG نهائياً** — ملف SVG نصّ يحتوي جافاسكربت قابلاً للتنفيذ. اقبل `jpg/png/webp` فقط.
2. **افحص البايتات الأولى (magic bytes)** لا الامتداد. `receipt.jpg` قد يكون HTML بالكامل.
3. **أعد ترميز الصورة على السيرفر** (sharp). هذا وحده يقتل أي حمولة مدسوسة، لأن الناتج صورة جديدة من البكسل.
4. **استبدل اسم الملف بـ UUID** — الاسم القادم من المستخدم قد يحمل `../`.
5. **دلو خاص + رابط موقّع مؤقّت** (دقائق معدودة)، لا رابط عام دائم.
6. `Content-Type` صحيح + `X-Content-Type-Options: nosniff`.
7. **أي نصّ داخل الصورة نصّ ميّت.** إن استُخرج بـ OCR فهو بيانات تُعرض وتُخزَّن كنصّ — لا يُبنى منه استعلام ولا يُحقن في صفحة.

### إضافات
- `Content-Security-Policy` صارمة.
- **حدّ محاولات الدخول على السيرفر** لا في المتصفح (قفل الـ5 دقائق في الواجهة يُتخطّى بتحديث الصفحة).
- الأدوار في جدول `profiles`، **لا** في `user_metadata` لأن الأخير قابل للتعديل من العميل.
- ممنوع إظهار أخطاء القاعدة الحقيقية للمستخدم. رسالة موحّدة + تسجيل في السيرفر.
- **الوقت من السيرفر دائماً** (`now()`), لا من جهاز المستخدم — وإلا مُدّدت التجربة بتغيير ساعة الهاتف.

---

## 2) نموذج البيانات

```
profiles            id → auth.users, role ('admin'|'merchant'|'staff'), created_at
restaurants         owner_id, name, owner_name, phone UNIQUE, email UNIQUE,
                    account_type ('trial'|'paid'), status ('pending'|'active'|'rejected'|'expired'),
                    trial_ends_at, subscription_ends_at
payments            restaurant_id, plan, amount, transfer_reference, receipt_path,
                    status ('pending'|'confirmed'|'rejected'), reviewed_by, reviewed_at
app_settings        صف واحد: rip_account, price_month, price_six_months, price_lifetime
materials           restaurant_id, name, unit, cost, stock, kind ('raw'|'goods'), price, category_id
categories          restaurant_id, name, parent_id, sauces_enabled
products            restaurant_id, name, price, category_id
product_parts       product_id, material_id, qty
workers             restaurant_id, name, job, wage_type ('fixed'|'var'), wage, earned, paid
orders              restaurant_id, no, status ('unpaid'|'cooking'|'served'|'rejected'|'expired'),
                    total, created_at, paid_at
order_lines         order_id, product_id, qty, sauces[]
```

### RLS
- `app_settings`: قراءة عامة (للسعر وRIP في صفحة الاشتراك)، كتابة للأدمن فقط.
- كل جداول المطعم: `restaurant_id IN (SELECT id FROM restaurants WHERE owner_id = auth.uid())`.
- الأدمن يرى كل شيء. حساب `staff` (كاشير/مطبخ) يرى **الطلبات فقط** — لا مخزون ولا تقارير ولا أرباح.

### قيود التحقّق
```sql
CHECK (stock >= 0)
CHECK (paid <= earned)            -- رصيد العامل لا يصبح سالباً
CHECK (price > 0 AND cost >= 0)
```

### التجديد تراكمي
```sql
subscription_ends_at = GREATEST(now(), subscription_ends_at) + interval '<مدة الباقة>'
```
فالتجديد المبكر لا يُضيّع الأيام المتبقّية.

### تأكيد الدفع — دالة واحدة ذرّية
`confirm_payment(order_id)` بصلاحية `SECURITY DEFINER`، داخل معاملة واحدة:
1. اقفل صفوف المواد المطلوبة (`FOR UPDATE`).
2. تحقّق أن كل مادة تكفي — إن نقصت واحدة، **أجهض العملية كلها** وأعد اسمها.
3. اخصم المواد.
4. حوّل حالة الطلب إلى `cooking`.

لا تفعل هذا بعدّة استعلامات من العميل — طلبان متزامنان يخصمان مخزوناً واحداً مرّتين.

---

## 3) قواعد التحقّق

| الحقل | القاعدة |
|---|---|
| اسم المطعم / المالك | 4 رموز فأكثر، محارف مسموحة، ليس أرقاماً فقط |
| اسم مادة/منتج/قسم/عامل | رمزان فأكثر، ليس أرقاماً فقط |
| الهاتف | يبدأ بـ `05` أو `06` أو `07` + 8 أرقام = 10، أرقام فقط |
| البريد | نمط صارم، 254 محرفاً كحد أقصى |
| كلمة السر | 8 رموز فأكثر، حروف **و** أرقام، بلا مسافات (يشمل حساب الموظّفين) |
| مرجع التحويل | أرقام فقط، 10 فأكثر |
| RIP بريدي موب | 20 رقماً بالضبط |
| الكميات والأسعار | `^\d+(\.\d{1,3})?$` |

المحارف المسموحة في الأسماء:
```js
/^[\u0621-\u064A\u0660-\u0669a-zA-Z0-9 .'\-()]+$/
```

---

## 4) الهوية البصرية

### الألوان
```css
--night:#14213D    /* النصّ والكتل الداكنة */
--bolt:#FFB703     /* الذهب */
--flame:#FB5607    /* اللهب — الأزرار الأساسية */
--good:#12805C     /* النجاح */
--bad:#C0341C      /* الخطأ */
--bg:#F6F8FC  --card:#FFFFFF  --mute:#68738C  --hair:#E4E9F2
```
تدرّج الأزرار الأساسية: `linear-gradient(135deg,#FB5607,#FFB703)`

### الخطوط
- العناوين والأرقام البارزة: **Baloo Bhaijaan 2** (700/800)
- النصّ: **Alexandria** (300/400/500)
- **لا خط أحادي المسافة** — جُرّب ورُفض، أعطى إحساساً بارداً.

### الشعار
دائري: قرص برتقالي متدرّج، إطار أسود، 13 لسان لهب حوله، `flash` بالأسود فوق `ordo` بالقرمزي `#9B1B00`، والبرق مكان الـ`s`.
الملف المرجعي: `flashordo-logo.jsx` — فيه نسخة كاملة ونسخة أيقونة بالبرق وحده للفافيكون.
تحت 56 بكسل استعمل نسخة الأيقونة، الحروف تذوب.

---

## 5) مصيدة RTL — تكرّرت مرّتين

الصفحة `dir="rtl"`، فأي نصّ لاتيني مقسّم إلى عناصر منفصلة **ينقلب ترتيبه**.
`flashordo` ظهر `ordohsalf`. نفس المشكلة تصيب البريد وRIP والهاتف في الجداول.

الحل — اعزل كل نصّ لاتيني:
```css
.ltr { direction:ltr; unicode-bidi:isolate; display:inline-block; }
```
استعمله في: الشعار، البريد الإلكتروني، RIP، أرقام الهاتف، مراجع التحويل.

---

## 6) الملفات الجاهزة

| الملف | المحتوى |
|---|---|
| `flashordo-onboarding-v3.jsx` | الترحيب، اختيار المسار، التجربة، الاشتراك، الباقات، الدفع، قيد المراجعة، الدخول |
| `flashordo-admin.jsx` | دخول الأدمن + الطلبات، المطاعم، الإيرادات، الإعدادات |
| `flashordo-merchant.jsx` | المخزون، المنتجات، العمّال، التقارير، متابعة، الشاشات |
| `flashordo-logo.jsx` | الشعار وحده مع معاينة كل الأحجام |

كلها **واجهات ببيانات وهمية**. مواضع الربط معلّمة بـ `TODO`.

---

## 7) المتبقّي

1. **SQL:** الجداول + RLS + قيود التحقّق + دالة `confirm_payment` الذرّية.
2. **الشاشات الثلاث:** الزبون (تابلت)، الكاشير، المطبخ — بحساب `staff` منفصل.
3. ربط الواجهات الأربع بـ Supabase.
4. دالة رفع الوصل الآمنة (بند 1 أعلاه).
5. مهمّة مجدولة: إلغاء الطلبات غير المدفوعة بعد 15 دقيقة، وإنهاء التجارب بعد 3 أيام.
6. حذف زر **«معاينة اللوحة (عرض تجريبي)»** من صفحة دخول الأدمن قبل النشر.

---

## 8) ملاحظات للتعامل مع المالك

- يفضّل المراجعة بالصور: اطلب لقطة شاشة بدل الوصف عند الخلاف على الشكل.
- الوصف المكتوب المفصّل الذي يرسله دقيق جداً — اتبعه حرفياً.
- لا تفترض؛ اسأل عن الغامض قبل البناء. راجع القواعد التسع في القسم 0 قبل كل ميزة جديدة.
